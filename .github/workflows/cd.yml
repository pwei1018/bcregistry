name: BCREGISTRY UI CD

on:
  push:
    branches:
      - reuseable-workflow
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment"
        required: true
        type: choice
        options:
        - dev
        - test
        - sandbox
        - prod
      tagname:
        description: "Specify the version (git tag) to deploy"
        required: false
        default: ""

jobs:
  bcregistry-cd:
    uses: pwei1018/bcregistry-sre/.github/workflows/ui-cd.yaml@reuseable-workflow
    with:
      environment: ${{ inputs.environment }}
      tagname: ${{ inputs.tagname }}
    secrets:
      APP_NAME: bcregistry
      OP_CONNECT_URL: ${{ secrets.OP_CONNECT_URL }}
      OP_CONNECT_TOKEN: ${{ secrets.OP_CONNECT_TOKEN }}
